apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: go-app-basic-auth
  namespace: ge-go-greeter
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: go-app
  basicAuth:
    users:
      name: basic-auth-users
